import Action from './Action';
export default class RunFunc extends Action {
    constructor(fn) {
        super();
        this.fn = fn;
    }
    tick(delta) {
        this.fn.call(this);
        return true;
    }
}
;
//# sourceMappingURL=RunFunc.js.map