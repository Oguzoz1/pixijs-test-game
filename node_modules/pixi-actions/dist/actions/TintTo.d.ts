import * as PIXI from 'pixi.js';
import TargetedAction from './TargetedAction';
import Interpolation from '../Interpolation';
export default class TintTo extends TargetedAction {
    interpolation: Interpolation;
    tintableTarget: PIXI.Sprite | PIXI.BitmapText | PIXI.Graphics | PIXI.Mesh;
    startTint: Array<number> | Float32Array;
    tint: Array<number> | Float32Array;
    constructor(target: PIXI.DisplayObject, tint: number, seconds: number, interpolation?: Interpolation);
    tick(delta: number): boolean;
    reset(): this;
}
